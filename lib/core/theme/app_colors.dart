import 'package:flutter/material.dart';

class AppColors {
  // Palet luxury
  static const Color navy = Color(0xFF1F2A44);
  static const Color warmBeige = Color(0xFFE8DCC8);
  static const Color softGold = Color(0xFFC6A75E);
  static const Color white = Color(0xFFFFFFFF);

  // Background aplikasi
  static const Color background = Color(0xFFF3EBDD);

  // Card utama
  static const Color card = white;
  static const Color cardWhite = white;

  // Warna utama
  static const Color primary = navy;
  static const Color primaryDark = navy;

  // Icon, link, aksen kecil
  static const Color secondary = navy;

  // Warna soft untuk dropdown, avatar, box icon
  static const Color softPrimary = Color(0xFFF0E6D6);

  // Warna card kecil seperti laporan terbaru
  static const Color softCard = Color(0xFFF5EFE6);

  // Warna gold untuk aksen / badge
  static const Color accent = softGold;

  // Teks
  static const Color textPrimary = navy;
  static const Color textSecondary = Color(0xFF8A8175);

  // Tombol utama
  static const Color buttonPrimary = navy;
  static const Color buttonText = white;

  // Status aktif
  static const Color successBg = Color(0xFFEFE7C8);
  static const Color successText = Color(0xFF6B5720);

  // Status tidak aktif / logout
  static const Color dangerBg = Color(0xFFFCEBEB);
  static const Color dangerText = Color(0xFFA32D2D);
}
